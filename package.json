{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "Test cypress",
  "main": "index.js",
  "scripts": {
    "start": "serve --listen ${PORT:-8080}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alberto",
  "license": "ISC",
  "dependencies": {
    "cypress": "^5.0.0"
  }
}
